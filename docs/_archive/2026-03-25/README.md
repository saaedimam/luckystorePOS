# Archived reference (2026-03-25)

Material here is **not part of the built Vite app** or active Edge Function sources.

- `edge-function-snapshot/` — historical copy of import logic; use `supabase/functions/import-inventory/index.ts` instead.
- `nextjs-reference/` — App Router snippets (cart, checkout, API routes) from an earlier integration sketch; production uses `apps/frontend` and `supabase/functions/payment-*`.
